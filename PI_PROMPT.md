# Build v1 with Pi

Read `.pi/prompts/build-v1.md` completely and execute it as the implementation request for this repository.

Treat `SPEC.md` as the product/architecture contract, `AGENTS.md` as the working contract, and `CONTEXT.md` as the canonical domain language. Use Bun exclusively. Build and verify locally; do not create remote Cloudflare resources or deploy.
